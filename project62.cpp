#include <iostream>
#include<string>

using namespace std;

int main()
{
    string str="welcome";
    
    // char str[10];
    
    // s.copy(str,s.length());               this copies the string into char array.
    // s.copy(str,7);
    
    
    
    cout<<str.find('c')<<endl;                  //this finds the index of char or string.

    return 0;
}
